
export class Logo {
  // Empty class, no longer needed as we're positioning the animation directly around the bg text
  static draw(ctx: CanvasRenderingContext2D, centerX: number, centerY: number) {
    // No longer drawing anything here
  }
}
